import { Typography } from '@mui/material';

const Logo = () => {
  return (
    <Typography variant="h4" align="center" sx={{ mb: '10px' }}>
      🎵 Ecstasify
    </Typography>
  );
};

export default Logo;
